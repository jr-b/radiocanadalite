<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Radio-Canada Lite | Version légère de Radio-Canada.ca">
  <title>{{ title }}{% if page.url != '/' %} | {{ meta.siteName }}{% endif %}</title>
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
  <link rel="stylesheet" href="{{ '/css/marquee.css' | url }}">
  

</head>
<body>

  {% include "nav.njk" %}
  
  {{ content | safe }}

  <footer>
    <div>
      <p>{{ meta.siteName }}{% if meta.authorName %} • {{ meta.authorName }}{% endif %}</p>
      <p class="buildtime">Dernière mise à jour: {{ buildtime.timestamp | dateToISO }} UTC</p>
    </div>
  </footer>
    <script src="/utils/mode-switcher.js"></script>

</body>
</html>